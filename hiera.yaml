---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:
  - name: "Distro release version"
    path: "el%{facts.operatingsystemmajrelease}.yaml"
  - name: "Common defaults"
    path: "common.yaml"
